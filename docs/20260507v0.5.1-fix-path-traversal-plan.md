# v0.5.1 Path Traversal 脆弱性修正 計画書（2026-05-07）

> **ステータス: ✅ 完了（2026-05-07）**
>
> 本計画書に基づく Phase 1〜4 の作業はすべて完了し、検証も全 19 試験項目（正常系 8 + 異常系 11 + モバイル 0）が Pass。CI も全 12 ジョブ（backend / frontend × 3 OS × 2 ランタイム）が green。
>
> - 作業ブランチ: `tominaga/20260507-v0-5-1-fix-path-traversal`
> - PR: [#20](https://github.com/elvezjp/coding-policy-ai-auditor/pull/20)
> - 関連 Issue: [#19](https://github.com/elvezjp/coding-policy-ai-auditor/issues/19)
>
> | Phase | 内容 | 主なコミット |
> |---|---|---|
> | 1 | v0.5 を複製して `versions/v0.5.1/` を作成 | `b0dab3a` |
> | 2 | `_safe_relative_path` の fallback 修正 + `_create_temp_files` の境界チェック追加 + テスト 8 件追加 | `f342662` |
> | 3 | v0.5.1 のメタデータ更新（pyproject / package.json / spec.md / README / configSchema / auditor） | `070bbdf` |
> | 4 | ルートドキュメント更新（README / CHANGELOG / SECURITY / CONTRIBUTING / versions/README） + CI workflow の対象バージョン変更 | `ad9c2ce`, `f56f986` |
> | 追加 | Windows 対応: `_safe_relative_path` で drive-relative パス（先頭 `/` `\`）を検出するよう拡張 + テスト 2 件追加 | `e2cc30b` |
>
> 検証結果: backend **151 / 151** passed（既存 141 + 新規 10）/ frontend 95 / 95 passed / build success / 手動確認（Web UI 経由の Java + Python 静的解析・LLM 監査の全体フロー）OK / CI 全 12 ジョブ green。

## 目的

[Issue #19](https://github.com/elvezjp/coding-policy-ai-auditor/issues/19) で指摘された `_safe_relative_path` メソッドの Path Traversal 脆弱性を、**v0.5.1 として新規ディレクトリで修正リリース**する。過去バージョン（v0.3, v0.4）は README の Dependabot Alert Policy に準じて修正対象外とする。

## 対象 Issue

| 番号 | タイトル | 種別 |
|------|---------|------|
| [Issue #19](https://github.com/elvezjp/coding-policy-ai-auditor/issues/19) | [Security] Path Traversal: `_safe_relative_path` の fallback に欠陥があり任意ファイル書き込みが可能 | security / fix |

## 前提・経緯

- v0.5.0 の `versions/v0.5/backend/app/services/static_analysis/base.py` 内 `_safe_relative_path` の fallback ロジックに欠陥
- `..` を含むパスを検知した際、fallback で同じ `name` フィールドをそのまま返すため、Path Traversal が成立
- 攻撃シナリオ: `POST /api/static-analysis/analyze` に `"name": "../../../tmp/evil.py"` を送ることで、tmpdir 外への任意ファイル書き込みが可能（認証不要、`.py` / `.java` 等の許可拡張子で動作）
- 深刻度: **HIGH**（信頼度 9/10）
- 過去バージョン（v0.3, v0.4）にも同一の問題があるが、本リポジトリの方針（README の Dependabot Alert Policy に準ずる）に従い修正対象外とする

## 方針

### バージョニング

- v0.5 ディレクトリは現行のまま残し、新規に **`versions/v0.5.1/`** を作成して修正版を提供する
- v0.5.1 は v0.5 を丸ごと複製したうえで、`base.py` のみを修正する

### 修正方針（多層防御）

Issue 中で提示された 2 つの修正案を **両方** 適用する。

#### 方法 1: `_safe_relative_path` の fallback でディレクトリ部分を除去

```python
def _safe_relative_path(self, file_data: dict[str, str]) -> Path:
    file_path = file_data.get("path") or file_data.get("name", "")
    candidate = Path(file_path)
    # Windows では Path("/etc/passwd").is_absolute() が False になるが、
    # tmpdir と join すると drive-relative で親ディレクトリへ抜けられるため、
    # 先頭スラッシュ系の root prefix も明示的に検出する
    has_root_prefix = file_path.startswith(("/", "\\"))
    if candidate.is_absolute() or has_root_prefix or ".." in candidate.parts:
        name_only = Path(file_data.get("name", "")).name
        return Path(name_only) if name_only else Path("unknown_file")
    return candidate
```

> **補足**: 当初は `is_absolute() or ".." in parts` のみだったが、CI の Windows backend-test で `/etc/passwd` 系のパスが検出されないことが判明し（`Path("/etc/passwd").is_absolute()` が False になるため）、`has_root_prefix` チェックを追加した（コミット `e2cc30b`）。Method 2 の境界チェックでも当該攻撃は防御できるが、Method 1 でも一貫した sanitize を行うようにすることで多層防御を強化している。

#### 方法 2: `_create_temp_files` で resolve 後の境界チェック

```python
resolved = target_path.resolve()
tmpdir_resolved = Path(tmpdir).resolve()
if not resolved.is_relative_to(tmpdir_resolved):
    raise ValueError(f"Path traversal detected: {target_path}")
```

## 作業内容

### Phase 1: ベースディレクトリ作成（完了）

| # | 内容 | 対象 |
|---|------|------|
| 1-1 | `versions/v0.5/` を `versions/v0.5.1/` に複製 | 全ファイル |
| 1-2 | 複製状態でコミット（`b0dab3a`） | — |

### Phase 2: 脆弱性修正（本計画の中心）

| # | 内容 | 対象ファイル |
|---|------|-------------|
| 2-1 | `_safe_relative_path` の fallback 修正（方法 1） | `versions/v0.5.1/backend/app/services/static_analysis/base.py` |
| 2-2 | `_create_temp_files` に resolve 後の境界チェック追加（方法 2） | 同上 |
| 2-3 | 既存テストへの影響確認 | `versions/v0.5.1/backend/tests/test_static_analysis.py` |
| 2-4 | Path Traversal 防御のテストケース追加（正常系・異常系） | 同上 |
| 2-5 | （追加対応）Windows での drive-relative パス検出を `_safe_relative_path` に追加し、回帰防止テストを 2 件追加（コミット `e2cc30b`） | 同上 |

### Phase 3: メタデータ更新

| # | 内容 | 対象ファイル |
|---|------|-------------|
| 3-1 | `pyproject.toml` の version を `0.5.1` に更新 | `versions/v0.5.1/backend/pyproject.toml` |
| 3-2 | `package.json` の version を `0.5.1` に更新 | `versions/v0.5.1/frontend/package.json` |
| 3-3 | `spec.md` 冒頭の「バージョン」表記を `v0.5.1` に更新 | `versions/v0.5.1/spec.md` |
| 3-4 | v0.5.1 配下の README に修正内容を追記 | `versions/v0.5.1/README.md` |
| 3-5 | フロントエンドの VersionSelector に v0.5.1 を追加 | `versions/v0.5.1/frontend/src/...`（必要に応じて） |

### Phase 4: ルートドキュメント更新

| # | 内容 | 対象ファイル |
|---|------|-------------|
| 4-1 | ルート [README.md](../README.md) / [README_ja.md](../README_ja.md) の実行手順・参照パス内のバージョン番号を `v0.5` → `v0.5.1` に更新（修正概要の追記は行わない） | 両ファイル |
| 4-2 | [versions/README.md](../versions/README.md) に v0.5.1 行を追加 | — |
| 4-3 | [CHANGELOG.md](../CHANGELOG.md) / [CHANGELOG_ja.md](../CHANGELOG_ja.md) に `[0.5.1]` セクションを追加（`### Security` で本修正、`[SECURITY]` プレフィックス付与） | 両ファイル |
| 4-4 | [SECURITY.md](../SECURITY.md) / [SECURITY_ja.md](../SECURITY_ja.md) の Supported Versions 表に `0.5.1` を追加（既存の `0.5.x` 表記で済む場合は注記のみ調整） | 両ファイル |
| 4-5 | [CONTRIBUTING.md](../CONTRIBUTING.md) / [CONTRIBUTING_ja.md](../CONTRIBUTING_ja.md) の手順・参照パス内のバージョン番号を `v0.5` → `v0.5.1` に更新 | 両ファイル |

#### Phase 4-1 / 4-5 の対象箇所（参考）

| ファイル | 行番号 | 対象 |
|---|---|---|
| `README.md` | 70, 80, 118, 121, 147, 229, 245, 256 | `cd versions/v0.5/...`、ディレクトリ説明、spec リンク、Dependabot 方針内の現行版表記 |
| `README_ja.md` | 70, 80, 118, 121, 147, 228, 244, 255 | 同上 |
| `CONTRIBUTING.md` | 45, 52, 90, 102, 162 | `cd versions/v0.5/...`、フォーカス対象バージョン |
| `CONTRIBUTING_ja.md` | 45, 52, 90, 102, 162 | 同上 |

> 注: Dependabot Alert Policy 内の「最新バージョン」表記（README L256 / README_ja L255）は本修正と直接の関連はないが、現行版が v0.5.1 に切り替わるため整合性を取るためにあわせて更新する。

## 検証

### 自動テスト

- [x] `versions/v0.5.1/backend` で `uv run pytest` がすべて pass
  - **結果**: 151 / 151 passed（既存 141 + 新規 10）。実行コマンド: `uv run pytest`
- [x] `versions/v0.5.1/backend/tests/test_static_analysis.py` がすべて pass
  - **結果**: 40 / 40 passed
- [x] 追加した Path Traversal 防御テストが期待どおり pass する
  - `TestSafeRelativePath` × 7 件 / `TestCreateTempFilesPathTraversal` × 3 件すべて pass
  - `name` に `../../../tmp/evil.py` を渡しても tmpdir 外に書き込まれないことを `test_traversal_via_name_does_not_escape_tmpdir` で確認
  - `path` が絶対パスの場合の fallback を `test_absolute_path_falls_back_to_name_basename` で確認
  - 正常な相対パスが従来通り動作することを `test_normal_subdir_still_works` で確認
  - Windows の drive-relative パス（`/etc/passwd` / `\Windows\System32\evil.py`）も fallback されることを `test_leading_slash_path_falls_back_even_when_not_absolute` / `test_leading_backslash_path_falls_back` で確認
- [x] フロントエンド `npm run test:run` がすべて pass
  - **結果**: 95 / 95 passed
- [x] フロントエンド `npm run build` が成功
  - **結果**: success（dist 生成確認済み）
- [x] CI（GitHub Actions）の全 12 ジョブが pass
  - **結果**: backend-test (3 OS × 2 Python) 全 6 件 + frontend-test (3 OS × 2 Node) 全 6 件すべて green
  - 注: 一度 frontend-test (windows-latest, 20) で `useTokenEstimation` の 5s タイムアウト fail（本修正と無関係の flaky test）が発生したが、再実行で pass

### 手動確認（プログラム呼び出しで等価検証）

> Claude Code 環境では HTTP サーバーを起動した POC を実行できないため、`_create_temp_files` を直接呼び出すスクリプトでエクスプロイトと同じ攻撃ペイロードを流して挙動を確認した。

- [x] エクスプロイト POC 相当の入力で tmpdir 外への書き込みが発生しないこと
  - `{"name": "../../../tmp/evil.py", "content": "import os"}` → tmpdir 内に `evil.py` のみ作成、`/tmp/evil.py` は不在
  - `{"name": "../../../tmp/Evil.java", "content": "class Evil {}"}` → tmpdir 内に `Evil.java` のみ作成、`/tmp/Evil.java` は不在
  - `{"name": "%2e%2e/%2e%2e/etc/x.py", "content": "x=1"}` → tmpdir 配下にリテラルディレクトリとして作成、`/etc/x.py` は不在
- [x] `/tmp` および `/etc` 配下に意図しないファイルが作成されていないこと
  - `ls /tmp/evil.py /tmp/Evil.java /etc/x.py /etc/foo.py /etc/passwd_clone` → 全件 `No such file or directory`

### 試験項目表

本修正の妥当性確認（Path Traversal が実際に防御されること）に加え、修正による既存機能への影響がないこと（静的解析実行・ファイル名処理・AI 監査の全体フロー）を確認する。

> **結果欄の凡例**:
> - ✅ Pass: Claude Code 環境上で確認済み（テスト pass / 等価検証 OK）
> - ⏸ 未実施: 外部環境（実 LLM・GUI 操作・実 CI ランナー等）が必要なため Claude Code 環境では未確認。マージ後に実環境で実施予定

#### 1. 正常系

| No | 試験項目 | 前提条件 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|---|
| N-001 | 通常の相対パスのファイルが tmpdir 内に作成されること | v0.5.1 バックエンド起動済み | 1. `_create_temp_files` に `[{"name": "src/app.py", "content": "x = 1"}]` を渡す | tmpdir 配下に `src/app.py` が作成される | ✅ Pass（`test_normal_subdir_still_works` で `pkg/sub/Foo.java` の階層作成を確認、同等のロジックを通過） |
| N-002 | `path` フィールド指定時に既存ファイルがコピーされること | UTF-8 の Java ファイルが存在 | 1. `[{"path": "/abs/path/Sample.java"}]` を渡す | tmpdir 配下に `Sample.java` がコピーされる | ✅ Pass（`test_cp932_file_converted_to_utf8` で `path` 指定経路を確認） |
| N-003 | CP932 エンコードのファイルが UTF-8 に変換されて保存されること | CP932 の Java ファイルが存在 | 1. `path` 指定で `_create_temp_files` を呼ぶ | tmpdir 配下に UTF-8 で書き戻される | ✅ Pass（`test_cp932_file_converted_to_utf8`） |
| N-004 | バイト型 content が正しく書き込まれること | なし | 1. `[{"name": "data.bin", "content": b"\x00\x01"}]` を渡す | tmpdir 配下に同内容のバイナリが作成される | ✅ Pass（プログラム呼び出しで `b"\x00\x01\x02"` がそのまま書き込まれることを確認） |
| N-005 | Java + Checkstyle の静的解析が従来通り動作すること | Checkstyle 利用可能 | 1. `POST /api/static-analysis/analyze` に正常な Java ファイル群を送信 | violations が返り `status: executed` になる | ✅ Pass（2026-05-07 手動確認: Web 画面で Java ファイルをアップロードし監査実行、静的解析タブに Checkstyle / PMD の violations が表示されることを確認） |
| N-006 | Python + Ruff の静的解析が従来通り動作すること | Ruff 利用可能 | 1. `POST /api/static-analysis/analyze` に正常な Python ファイル群を送信 | violations が返り `status: executed` になる | ✅ Pass（2026-05-07 手動確認: Web 画面で Python ファイルをアップロードし監査実行、静的解析タブに Ruff / Pylint の violations が表示されることを確認） |
| N-007 | 静的解析→AI 監査の全体フローが従来通り動作すること | LLM 設定済み | 1. フロントエンドから規約 + コードをアップロードして「監査実行」 | 結果画面に静的解析結果と監査結果 #1/#2 が表示される | ✅ Pass（2026-05-07 手動確認: 規約 + コードをアップロードして監査実行、静的解析結果と LLM 監査結果 #1 / #2 が結果画面に表示されることを確認） |
| N-008 | サブディレクトリを含む相対パスが階層を保って作成されること | なし | 1. `[{"name": "pkg/sub/Foo.java", "content": "..."}]` を渡す | tmpdir 配下に `pkg/sub/Foo.java` が作成される | ✅ Pass（`test_normal_subdir_still_works`） |

#### 2. 異常系

| No | 試験項目 | 前提条件 | 手順 | 期待結果 | 結果 |
|---|---|---|---|---|---|
| E-001 | `name` に `..` を含むパスでも tmpdir 外に書き込まれないこと | v0.5.1 バックエンド起動済み | 1. `[{"name": "../../../tmp/evil.py", "content": "import os"}]` を `POST /api/static-analysis/analyze` に送信 | tmpdir 配下に `evil.py` のみが作成される、または `ValueError` で拒否される。`/tmp/evil.py` は作成されない | ✅ Pass（`test_traversal_via_name_does_not_escape_tmpdir` + `/tmp/evil.py` 不在を `ls` で確認） |
| E-002 | `path` が絶対パスでも tmpdir 外に書き込まれないこと | なし | 1. `[{"name": "/etc/passwd_clone", "content": "..."}]` を送信 | tmpdir 配下に sanitize された名前で作成、または拒否される。`/etc` 配下は変更されない | ✅ Pass（`test_absolute_path_falls_back_to_name_basename` で fallback ロジックを確認、`/etc/passwd_clone` 不在を `ls` で確認） |
| E-003 | 多重 `..` を含むパスでも tmpdir 外に解決されないこと | なし | 1. `[{"name": "a/../../../etc/foo.py", "content": "..."}]` を送信 | resolve 後の境界チェックで `ValueError` が発生する | ✅ Pass（`test_resolve_boundary_check_blocks_synthetic_traversal` で `ValueError("Path traversal detected")` を確認、`/etc/foo.py` 不在を `ls` で確認） |
| E-004 | `name` が空文字列の場合に `unknown_file` で安全に処理されること | なし | 1. `[{"name": "", "path": "../../etc/x"}]` を送信 | tmpdir 配下に `unknown_file` として作成される、または拒否される | ✅ Pass（`test_empty_name_fallback_returns_unknown_file`） |
| E-005 | URL エンコード済みの `..` が誤って素通しされないこと | なし | 1. `[{"name": "%2e%2e/%2e%2e/etc/x.py", "content": "..."}]` を送信 | `Path.parts` 上は `..` ではないが、resolve 後の境界チェックで安全側に倒れる（tmpdir 内に収まるか拒否） | ✅ Pass（プログラム呼び出しで tmpdir 配下に `%2e%2e/%2e%2e/etc/x.py` がリテラルディレクトリとして作成されることを確認、`/etc/x.py` 不在を `ls` で確認） |
| E-006 | content と path の両方が欠落している場合に従来通り `ValueError` になること | なし | 1. `[{"name": "x.py"}]` を送信 | `ValueError("file spec must contain 'path' or 'content'")` で拒否 | ✅ Pass（プログラム呼び出しで `ValueError("file spec must contain 'path' or 'content'")` を確認） |
| E-007 | content の型が不正な場合に従来通り `TypeError` になること | なし | 1. `[{"name": "x.py", "content": 123}]` を相当する内部呼び出しで実行 | `TypeError("content must be bytes or str")` が発生 | ✅ Pass（プログラム呼び出しで `TypeError("content must be bytes or str")` を確認） |
| E-008 | 既存の Path Traversal 攻撃ベクター（Java / 他拡張子）でも防御できること | なし | 1. `[{"name": "../../../tmp/Evil.java", "content": "class Evil {}"}]` を送信 | tmpdir 外に `Evil.java` が作成されない | ✅ Pass（プログラム呼び出しで tmpdir 内のみに `Evil.java` 作成、`/tmp/Evil.java` 不在を `ls` で確認） |
| E-009 | 修正後も `_safe_relative_path` 単体テストの境界値が期待通り動作すること | なし | 1. v0.5.1 の `tests/test_static_analysis.py` を `uv run pytest` で実行 | 既存テスト＋追加テストがすべて pass | ✅ Pass（40 / 40 passed。バックエンド全体では 151 / 151 passed） |
| E-010 | Windows の drive-relative パス（`/etc/passwd` 形式）でも fallback が発動すること | なし | 1. `_safe_relative_path({"path": "/etc/passwd", "name": "passwd"})` を呼ぶ | `Path("passwd")` が返り、tmpdir に basename で作成される。`is_absolute()` が False を返す Windows でも fallback が発動する | ✅ Pass（`test_leading_slash_path_falls_back_even_when_not_absolute`。CI Windows backend-test も green） |
| E-011 | Windows のバックスラッシュ root prefix（`\Windows\System32\evil.py`）でも fallback が発動すること | なし | 1. `_safe_relative_path({"path": "\\Windows\\System32\\evil.py", "name": "evil.py"})` を呼ぶ | `Path("evil.py")` が返る | ✅ Pass（`test_leading_backslash_path_falls_back`） |

#### 3. モバイル

（該当する試験項目なし：0件）

> 本修正はバックエンドの内部メソッド（`_safe_relative_path` / `_create_temp_files`）に閉じた Python の変更であり、フロントエンドの UI／タッチ操作／レスポンシブには一切影響しないため、モバイル特有の試験項目は発生しない。

#### 試験結果サマリー

| カテゴリ | 件数 | ✅ Pass | ⏸ 未実施 |
|---|---|---|---|
| 正常系 | 8 | 8 | 0 |
| 異常系 | 11 | 11 | 0 |
| モバイル | 0 | — | — |
| **合計** | **19** | **19** | **0** |

全 19 項目の確認が完了。

- N-005 / N-006 / N-007 は 2026-05-07 に v0.5.1 を起動した Web 画面から Java + Python の規約・コードをアップロードして監査実行を行い、静的解析と LLM 監査が問題なく動作することを確認した。
- E-010 / E-011 は CI の Windows backend-test 失敗を契機に追加した Windows 固有のケース。`_safe_relative_path` を `has_root_prefix` で拡張したうえでテストを追加し、ローカル POSIX と CI Windows の両環境で pass を確認した。

## 影響範囲

| 観点 | 影響 |
|------|------|
| API 仕様 | 変更なし（不正パスは従来 silent fallback → **明示的に拒否 or sanitize**） |
| 正常系の挙動 | 変更なし |
| 異常系の挙動 | Path Traversal を試みるリクエストは `ValueError` で拒否 |
| 旧バージョン互換 | v0.5 はそのまま残置、利用者は v0.5.1 へ切り替えが必要 |

## 対象外

- v0.3, v0.4 の同一脆弱性の修正（README の方針に従い対象外）
- 認証機構の追加（本 Issue のスコープ外）
- chroot 等の OS レベル防御（同上）

## 対象ファイル一覧

```
docs/
└── 20260507v0.5.1-fix-path-traversal-plan.md   ... 本計画書（新規）

versions/v0.5.1/                                  ... v0.5 から複製（新規）
├── backend/app/services/static_analysis/base.py  ... 2-1, 2-2
├── backend/tests/test_static_analysis.py         ... 2-3, 2-4
├── backend/pyproject.toml                        ... 3-1
├── frontend/package.json                         ... 3-2
├── spec.md                                       ... 3-3
└── README.md                                     ... 3-4

README.md, README_ja.md                           ... 4-1
versions/README.md                                ... 4-2
CHANGELOG.md, CHANGELOG_ja.md                     ... 4-3
SECURITY.md, SECURITY_ja.md                       ... 4-4
CONTRIBUTING.md, CONTRIBUTING_ja.md               ... 4-5
```

## 関連

- [Issue #19](https://github.com/elvezjp/coding-policy-ai-auditor/issues/19)（本件）
- 作業ブランチ: `tominaga/20260507-v0-5-1-fix-path-traversal`
