# v0.5 修正計画書（2026-04-16）

## 目的

Windows 環境での静的解析モジュールの動作不具合（Issue #13）およびフロントエンドテストの不安定性（Issue #15）を修正し、v0.5 としてリリースする。

## 対象 Issue / PR

| 番号 | タイトル | 種別 |
|------|---------|------|
| [Issue #13](https://github.com/elvezjp/coding-policy-ai-auditor/issues/13) | Windows対応: 静的解析モジュールの .bat/.cmd 実行・エンコーディング問題 | bug |
| [Issue #15](https://github.com/elvezjp/coding-policy-ai-auditor/issues/15) | フロントエンドテスト失敗: addCodeFiles でファイル再選択時に既存ファイルがリセットされない | bug |
| [PR #14](https://github.com/elvezjp/coding-policy-ai-auditor/pull/14) | Windows対応の実装（Issue #13 の改修案） | PR |

## 前提・経緯

- PR #14（atsutakaGithub 氏による改修案）は `versions/v0.4` に対して実装されていた
- 本件対応として、v0.4 を変更せず新たに `versions/v0.5` ディレクトリを作成して管理する方針とした
- PR #14 のブランチ `takahashi/20260331-issue-13-windows` 上で作業を行い、以下を実施済み:
  1. `versions/v0.4` → `versions/v0.5` にリネーム（`git mv`）
  2. `main` ブランチからオリジナルの `versions/v0.4` を復元
  3. `versions/v0.5` 内のバージョン番号を v0.4.0 → v0.5.0 に一括更新

## 修正内容

### Issue #13: Windows 対応（PR #14 ベース）

PR #14 で既に取り込み済みの対応（問題1・問題2）はそのまま活用し、Issue #13 で指摘された問題3（バグ・コード品質問題）を追加修正する。

#### 取り込み済み（PR #14 の成果）

| 問題 | 対応内容 | 対象ファイル |
|------|---------|-------------|
| 問題1: `.bat/.cmd` 実行不可 | `resolve_cmd_on_windows()` を `utils/proc.py` に新規追加。`shell=True` を使わず `["cmd", "/c", ...]` で安全に実行 | `utils/proc.py`（新規） |
| 問題2: エンコーディングエラー | `run_capture()` で UTF-8 → CP932 → デフォルト → replace のフォールバックデコード。`_create_temp_files()` に CP932→UTF-8 変換を追加 | `utils/proc.py`（新規）, `base.py` |
| subprocess 統一 | `tool_checker` / `checkstyle` / `pmd` の `subprocess.run()` を `run_capture()` に統一 | `tool_checker.py`, `checkstyle.py`, `pmd.py` |

#### 追加修正が必要な項目（問題3）

| # | 内容 | 対象ファイル | 重要度 |
|---|------|-------------|--------|
| 3-1 | `timer` の参照バグ（L60）: Java ファイル 0 件時の早期リターンで未定義の `timer` を参照。`duration_ms=None` に修正 | `tools/checkstyle.py` | 高 |
| 3-2 | デバッグコードの残置（L92-97）: `# @@@テスト用` コメントと 4000 文字のデバッグログ出力を削除 | `tools/checkstyle.py` | 高 |
| 3-3 | 未使用インポートの削除: `checkstyle.py` の `import shutil`, `from datetime import datetime`、`tool_checker.py` の `Dict, List, Optional, Tuple` | `tools/checkstyle.py`, `tool_checker.py` | 低 |
| 3-4 | 例外処理の整理（L67, L156）: `except (FileNotFoundError, Exception)` → `TimeoutExpired` を区別できるよう分離 | `tool_checker.py` | 中 |

### Issue #15: フロントエンドテストの不安定性

| # | 内容 | 対象ファイル |
|---|------|-------------|
| 15-1 | `addCodeFiles` のテストで `setTimeout(0)` による非同期待ちが不十分で、2回目の呼び出し結果が1回目に上書きされる。`waitFor` を使った安定的な待ち方に変更 | `useFileConversion.test.ts` |
| 15-2 | （必要に応じて）実装側で `addCodeFiles` 呼び出し時に `setCodeFiles([])` でリセットしてから非同期処理を開始する | `useFileConversion.ts` |

## 対象ファイル一覧

```
versions/v0.5/
├── spec.md                             ... S-1〜S-6
├── backend/
│   └── app/services/static_analysis/
│       ├── tool_checker.py             ... 3-3, 3-4
│       ├── tools/
│       │   └── checkstyle.py           ... 3-1, 3-2, 3-3
│       └── utils/
│           └── proc.py                 ... (取り込み済み)
└── frontend/
    └── src/
        ├── features/auditor/hooks/
        │   └── useFileConversion.ts        ... 15-2
        └── __tests__/features/auditor/hooks/
            └── useFileConversion.test.ts   ... 15-1
```

## 仕様書（spec.md）の修正箇所

仕様駆動開発の工程に従い、実装に先立って仕様書を更新する。

### S-1: F-05 機能詳細にWindows対応・エンコーディング仕様を追記（L132-139）

現状の F-05 にはツール名と実行タイミングしか記載がなく、Windows での実行方式やエンコーディング処理について言及がない。

**現状:**
```
| 項目 | 内容 |
|------|------|
| Java用ツール | Checkstyle, PMD |
| Python用ツール | Ruff, Flake8, Pylint |
| 実行タイミング | AI監査前（オプション） |
| 結果連携 | 静的解析サマリーをLLMコンテキストに含める |
```

**追記内容:**

| 項目 | 追記内容 |
|------|---------|
| Windows対応 | `.bat/.cmd` 形式のツールを `cmd /c` 経由で安全に実行（`shell=True` 不使用） |
| エンコーディング | ツール出力を UTF-8 → CP932 → デフォルト → replace の順でフォールバックデコード |
| ファイルエンコーディング変換 | 解析対象ファイルが CP932 の場合、一時ファイルに UTF-8 で書き戻してからツールに渡す |

### S-2: F-01 機能詳細にファイル再選択時の動作を明記（L60-67）

現状の F-01 にはファイル再選択時の振る舞いが未定義。Issue #15 で発覚した通り、再選択時に既存ファイルがリセットされるべきかどうかの仕様が必要。

**追記内容:**

| 項目 | 追記内容 |
|------|---------|
| ファイル再選択 | プログラムファイルを再選択した場合、既存のファイル一覧を新しい選択結果で置換する |

### S-3: 8.2 前提条件の Python バージョンを修正（L811）

現状は「Python 3.10以上」と記載されているが、`pyproject.toml` の `requires-python = ">=3.11"` と不整合。

**修正:**
- 現状: `Python 3.10以上がインストールされていること`
- 修正後: `Python 3.11以上がインストールされていること`

### S-4: 8.2 前提条件に Windows 動作環境の記載を追加（L808-813）

v0.5 で Windows 対応を行うため、対応 OS を明記する。

**追記内容:**

| 項目 | 追記内容 |
|------|---------|
| 対応OS | macOS, Linux, Windows（Windows では静的解析ツールが `.bat/.cmd` 形式で提供される場合がある） |

### S-5: 7.1 試験項目に Windows 固有テスト・エンコーディングテストを追加

#### F-05 静的解析実行に追加（L738-743 の後）

| No | 試験項目 | 試験内容 | 期待結果 |
|----|----------|----------|----------|
| F-05-05 | Windows .bat実行 | Windows で .bat 形式の Checkstyle を実行 | 正常に解析結果が返る |
| F-05-06 | CP932エンコーディング | CP932 の日本語 Java ファイルを解析 | UnicodeDecodeError が発生せず結果が返る |
| F-05-07 | Javaファイル0件 | Java ファイルが含まれない状態で Checkstyle 実行 | 空の violations で正常終了する |
| F-05-08 | config未存在 | 存在しない config ファイルを指定 | `skipped_config_missing` が返る |

#### F-01 ファイルアップロードに追加（L700-706 の後）

| No | 試験項目 | 試験内容 | 期待結果 |
|----|----------|----------|----------|
| F-01-07 | ファイル再選択 | プログラムファイルを選択後、別のファイルを再選択 | 既存ファイルが新しい選択結果で置換される |

#### 互換性試験に追加（L790-795 の後）

| No | 試験項目 | 試験内容 | 期待結果 |
|----|----------|----------|----------|
| C-05 | Windows バックエンド | Windows 環境でバックエンドテスト実行 | 全テストが通る |

### S-6: 変更履歴セクションを削除（L829-833）

変更履歴は git log で追跡可能なため、仕様書内の変更履歴セクションを廃止する。

---

## 作業手順

### 0. 仕様書の更新（v0.5 spec.md）

- [ ] S-1: F-05 に Windows 対応・エンコーディング仕様を追記
- [ ] S-2: F-01 にファイル再選択時の動作を追記
- [ ] S-3: 前提条件の Python バージョンを 3.11 に修正
- [ ] S-4: 前提条件に対応 OS を追記
- [ ] S-5: 試験項目に F-05-05〜08, F-01-07, C-05 を追加
- [ ] S-6: 変更履歴セクションを削除

### 1. Issue #13 問題3 の修正（v0.5 backend）

- [ ] 3-1: `checkstyle.py` の `timer` バグを修正
- [ ] 3-2: `checkstyle.py` のデバッグコードを削除
- [ ] 3-3: 未使用インポートを削除（`checkstyle.py`, `tool_checker.py`）
- [ ] 3-4: `tool_checker.py` の例外処理を `TimeoutExpired` と `FileNotFoundError` を分離
- [ ] バックエンドテスト実行・確認

### 2. Issue #15 の修正（v0.5 frontend）

- [ ] 15-1: テストの非同期待ちを `waitFor` に変更
- [ ] 15-2: 必要に応じて実装側を修正
- [ ] フロントエンドテスト実行・確認

### 3. CI・ドキュメント整備

- [ ] GitHub Actions に `windows-latest` テストを追加（Issue #13 対応方針4）
- [ ] `versions/README.md` に v0.5 の説明を追加
- [ ] PR 作成（Closes #13, Closes #15）

## 備考

- PR #14 の貢献者（atsutakaGithub 氏）へのクレジットを PR description および commit message に記載する
- PR #14 は本 PR のマージ後に Close（not merge）とする
